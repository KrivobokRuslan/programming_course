<?php  // 12 с помощью цикла for заполните массив числами от N до M. вернуть массив.
$n = 1;
$m = 4;
$mass = [];

    for ($i = $n; $i <= $m; $i++){
        $mass[$i] = $i;
    echo $mass[$i] . "<br>";
}
    ?> // 12 с помощью цикла for заполните массив числами от N до M. вернуть массив.